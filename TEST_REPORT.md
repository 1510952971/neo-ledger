# Neo Ledger 全量功能测试报告

日期：2026-07-29 · 方式：通过 ESLint、TypeScript 和生产构建检查；在隔离沙盒中以真实 SQLite 直接驱动 API 路由；使用 Android SDK 35 构建并校验 APK；实际构建并启动 Docker 镜像。共 150 项 API 断言 + 97 项网页单元测试 + Android 单元测试。

## 结论

247 / 247 网页与 API 测试通过。Android 37 项 Gradle 构建任务通过，APK v2 签名有效；Docker 健康接口返回当前版本且主页 HTTP 200。生产构建、数据落库、余额触发器、并发版本冲突、权限边界、验证码与头像安全、账号数据隔离和备份恢复回环均验证一致。

## 覆盖范围（36 个 API 路由直接触达）

**套件1 · 核心记账链路（38 项）**
账本增查、账户增删改查与四类非法输入拒绝、负债余额符号、改余额自动生成"余额调账"转账、默认分类播种、自定义分类、离线记账两笔入库、金额元转分精确（35.5→3550）、重复 offlineId 幂等去重、账单编辑、过期版本返回 409 冲突、账户转账触发器双向精确到分、分类预算读写、FIRE/通胀设置、偏好与主题、汇率、现金流预测、通知、JSON 导出（含 syncId 富化）、CSV 导出表头。

**套件2 · 业务模块（46 项）**
储蓄目标存入与账户联动扣款、负数金额拒绝；订阅创建、非法周期拒绝、到期自动扣费生成流水并推进下次扣费日；分期创建、绑定资产账户拒绝、到期自动还款两期、已还款分期删除保护、无还款分期可删；成员与人情平账；数码资产折旧估值、变卖回款生成收入流水；集成令牌签发、外部快速记账、错误令牌 401；webhook 文本解析入待确认、待确认转正式入账；账单导入去重（同 externalId 只入一笔）；P2P 发现/信令收发/CRDT 全量拉取；AI 本地统计问答；升级健康检查。

**套件3 · 恢复/账号/安全（66 项）**
备份恢复回环（恢复后流水、账户名、余额与导出前逐分一致）、非法备份拒绝；注册、会话 Cookie、错误密码 401、重名 409；注册后 localhost 匿名回退关闭（401）、旧 local 数据自动过户给首个账号、登出后旧 Cookie 失效、重新登录；登录用户访问自有账本的代理鉴权回归；账号头像迁移、持久化、重登读取、会话隔离、清除与 OAuth 同步策略，拒绝 SVG、任意 URL、伪装格式和超限图片；WebDAV 未登录 401、HTTP 明文地址拒绝、空备份拒绝；邮件服务状态、验证码用途与邮箱校验、防枚举响应、绑定发码、哈希存储、重发限流、错误尝试计数和无有效验证码时拒绝重置。

**项目自带单元测试**：97 / 97 通过，包括 SQLite 备份恢复、同步加密、账单解析、账号与验证码核心规则、侧栏账号关联、OAuth 状态、主题对比度、滚动行为、桌面启动器、附近同步、云同步与更新版本契约。

**Android 自动记账伴侣**：通知识别与过滤单元测试通过；`testDebugUnitTest` 与 `assembleDebug` 成功；最终 APK 使用 Android APK Signature Scheme v2 签名并通过 `apksigner verify`。尚需在真实品牌手机上继续验证微信/支付宝实际通知文案和各厂商后台策略。

**Docker / NAS**：`docker compose config` 通过；镜像构建成功；容器使用 Wrangler 本地 Worker 运行时和持久化 D1 数据卷启动；`/api/app-update/health` 返回 `v1.1.0`，主页返回 HTTP 200。

## 测试中发现并已修复

1. `app/api/transfers/route.ts`：`TransferKind` 类型导入缺 `type` 关键字。vite 构建下无影响，属潜在隐患，已修复并通过类型检查。

## 测试期间确认的正确保护行为（非 bug）

首次建库自动播种演示账户；有还款流水的分期拒绝直接删除；分期必须绑定负债账户；账单编辑携带版本号防并发覆盖；导入按 externalId 与"金额+时刻"双重去重。

## 浏览器界面测试（补充轮次）

在一个临时的“测试专用账本”里走完主流程，测完已删除，未触碰真实账本数据：新建账本 → 新增账户（¥10,000）→ 记一笔支出（¥88.88，餐饮）→ 账单明细核对 → 净资产核对 → 管理规划 → 统计分析 → 删除账本。

结果：全部通过。账户余额精确变为 ¥9,911.12，账单明细的时间、账户、分类、金额均正确，预算进度、FIRE 指数、统计图表同步刷新，删除账本后自动切回原账本且数据完好。控制台无应用级报错（仅有 4 条 Vite HMR 热更新失败提示，来自本次改动过程中的热替换，刷新后消失，不影响正式构建）。

### 本轮发现并修复的问题

1. **原生对话框会静默失效（重要）**：`新建账本`、`删除账本`、`恢复备份`、`清理账单` 等 20 处使用 `window.alert / confirm / prompt`。原生对话框会阻塞页面线程，且 Chrome 在连续弹出后会给用户“阻止此页面创建更多对话框”的勾选项——一旦勾选，这些流程点了没有任何反应，也没有错误提示。已全部替换为应用内对话框（危险操作用红色确认样式），提示类改用页面右上角 toast。
2. **应用内对话框被压在其他弹窗下方**：初版用普通 `div` + `z-index` 实现，但项目其他弹窗使用 `<dialog showModal()>` 渲染在浏览器顶层，任何 z-index 都盖不过。已改为同样使用 `<dialog>`。
3. **FIRE 圆环排版**：数字与“安全躺平指数”被分别推向圆环上下边缘，已改为整体居中并收紧字号。
4. **里程碑标签被裁切**：“半年备用金”等贴边标签会被容器裁掉，已用 `clamp()` 限制节点位置，两端标签均可完整显示。

## 未覆盖项（及原因）

微信/支付宝 OAuth 真实回调（需外网与开放平台密钥）；WebDAV 真实上传下载（需外部服务器，仅验证了安全校验路径）；一键升级完整流程（依赖 git/GitHub Release，其备份与版本规则已有单测覆盖）；不同品牌 Android 真机的支付通知与后台保活；iOS 快捷指令真机流程；PDF/OCR 账单解析的重型路径（tesseract/pdfjs，解析后入库逻辑已覆盖）。39 个 API 路由中，`app-update`、`auth/oauth` 与 `auth/oauth/callback` 未在 API 套件中直接驱动，其核心规则分别由更新与 OAuth 单元测试覆盖。

## 复现方式

测试装置位于仓库 `tests/api/`（D1→node:sqlite 兼容层 + 模块解析器 + 三个套件），并已接入 `npm test`。可用 `npm run test:api` 单独运行 API 套件。
