# Neo Ledger 移动端精品化完善计划

更新时间：2026-09-23  
执行起点：Native `1.4.0+21`  
适用平台：Android、iPhone、Android 平板、iPad  
关联平台：Web、Windows、macOS（共享账号、账本数据、API 与同步规则）

## 1. 目标与设计原则

本计划的目标是把移动端从“桌面网页的移动容器”完善为适合手机单手操作、可长期使用的原生记账应用，同时保留 Neo Ledger 现有的深色界面、荧光绿色品牌色、卡片语言和跨平台数据能力。

钱迹等成熟记账产品只用于参考通用的信息架构和交互习惯，例如快速记账、月份切换、日期分组、分类宫格、原生设置列表和明确的空状态；不复制其品牌、视觉资产、文案、图标或专有功能设计。

实施过程中必须遵守以下原则：

1. 移动端、Web、Windows 和 macOS 使用同一个 AWS 服务、域名、账号、账本与 API，不能产生独立数据孤岛。
2. 移动端可以采用更适合触控的导航和页面结构，但金额口径、业务规则、权限和同步结果必须一致。
3. 所有新建、编辑、删除、转账和自动识别写入都必须支持幂等、离线队列、失败提示和恢复。
4. 原生页面完成前可以保留网页端高级管理入口，但不得把跳转网页标记为“原生功能已完成”。
5. Android 与 iOS 的系统能力不同，自动记账使用各自允许的实现，不伪造跨平台能力一致。
6. 每个阶段必须完成代码、自动化测试、构建验证和真机验收后才能勾选完成。

## 2. 当前基线

Native `1.4.0+21` 已完成第一阶段原生壳层：

- Android/iOS 启动后进入 Flutter 原生移动界面，不再默认打开完整桌面 WebView。
- 已有首页、账单、分析、我的四个底部栏目和中央“记一笔”按钮。
- 已有支出/收入切换、数字键盘、分类选择、账户选择和备注输入。
- 已接入现有账本 API、会话恢复、离线写入队列和云端刷新。
- Web、Windows、macOS 继续使用统一网页端，不受移动布局影响。

当前仍属于“原生可用骨架”，以下内容尚未达到正式精品应用标准：

- 转账、预算、高级功能、隐私与安全仍有占位入口。
- 账单缺少详情、编辑、删除、搜索、月份切换和组合筛选。
- 记账缺少日期、标签、图片、报销、优惠、参与人和连续记账。
- 资产、自动记账、设置、头像和安全能力尚未全部原生化。
- iOS 工程尚未完成可构建、签名和真机验收闭环。

## 3. 总体执行顺序

必须按下列顺序推进。后一阶段可以提前做技术调研，但不能绕过前一阶段的验收门槛直接宣称完成。

| 顺序 | 阶段 | 主要结果 | 建议版本 |
| ---: | --- | --- | --- |
| 0 | 基础工程与质量基线 | 路由、状态、同步、测试和设计令牌稳定 | `1.4.x` |
| 1 | 完整记账体验 | 手机单手完成常用收支记录 | `1.5.0` |
| 2 | 账单浏览与管理 | 可查、可筛、可编辑、可删除、可撤销 | `1.6.0` |
| 3 | 首页与日常决策 | 首页展示现金流、预算、提醒和待处理事项 | `1.7.0` |
| 4 | 账户、转账与资产 | 原生账户和净资产闭环 | `1.8.0` |
| 5 | 预算、周期与计划 | 预算、订阅、分期、储蓄目标原生化 | `1.9.0` |
| 6 | 自动记账与智能导入 | Android 自动识别、iOS 分享/快捷指令、截图 AI | `1.10.0` |
| 7 | 我的、设置与安全 | 账号资料、头像、应用锁、设备与数据管理 | `1.11.0` |
| 8 | 精品化、平板与无障碍 | 动效、触觉、响应式、可访问性和性能完成 | `1.12.0` |
| 9 | 真机矩阵与正式发布 | Android/iOS 发布、升级和回滚闭环 | `2.0.0` |

版本号是执行建议；如某阶段拆分为多个小版本，阶段验收口径不变。

## 4. 阶段 0：基础工程与质量基线

### 4.1 任务清单

- [ ] 将移动端页面按 `core/domain/data/features/platform` 拆分，避免继续扩大单文件界面代码。
- [ ] 建立统一路由，覆盖首页、记账、账单详情、账户、资产、规划、自动记账和设置。
- [x] 抽取颜色、字号、圆角、间距、阴影、动画时长和金额样式为移动端设计令牌。
- [ ] 将 API 请求、会话、离线快照、写入队列和同步状态分离为可测试的数据层。
- [ ] 为写入操作统一增加 `idempotencyKey`、提交状态和错误映射。
- [ ] 建立页面级加载、空状态、错误状态、离线状态和重试组件。
- [ ] 修复并验证 iOS Flutter 工程配置，使未签名模拟器构建能够通过。
- [ ] 建立手机尺寸截图基线：小屏 Android、主流 Android、iPhone、平板竖屏、平板横屏。

### 4.2 验收标准

- `flutter analyze` 无错误和警告。
- Flutter 单元测试和 Widget 测试全部通过。
- Android debug/release 构建通过；iOS simulator 构建通过。
- 断网启动时可显示最近快照，不出现无限加载。
- 会话失效后返回登录页，不清除尚未同步的本地账单。
- 现有 Web、Windows、macOS 行为没有回归。

## 5. 阶段 1：完整记账体验

这是最高优先级。完成前不扩展装饰性首页模块。

### 5.1 金额与类型

- [x] 支出、收入、转账三种类型使用统一的顶部切换控件。
- [x] 数字键盘支持小数、退格、清空、加减连续计算和最大金额限制。
- [x] 输入数字时提供可关闭的轻触震动和金额过渡动画。
- [x] 防止 `0`、负数、超长小数和超出服务端范围的金额提交。
- [x] 保存按钮持续显示当前类型和金额，提交过程中禁止重复点击。

### 5.2 分类与账户

- [ ] 显示最近使用分类、一级分类和二级分类（最近分类已完成，二级分类等待服务端层级模型）。
- [x] 分类支持图标、颜色、搜索和用户自定义排序。
- [x] 记住每种分类最近使用的账户，但不静默覆盖用户本次选择。
- [x] 账户选择显示余额、币种和账户类型。
- [x] 收入和支出分别使用自己的分类集合。

### 5.3 账单字段

- [x] 日期和具体时间，提供今天、昨天、前天快捷入口。
- [ ] 商户/标题、备注、标签、参与人和情绪/刚需属性。
- [ ] 报销状态、优惠金额和不计入收支/预算的标记。
- [ ] 账单图片支持拍照、相册、压缩、预览、删除和上传失败重试。
- [ ] 多币种账单显示原币金额、汇率和本位币金额。
- [x] 连续记账模式：保存后清空金额，保留用户选择的账户和日期。

### 5.4 保存与离线

- [x] 保存后立即乐观显示在账单列表，并标记同步状态。
- [x] 离线保存进入本地队列，恢复网络后自动补发。
- [x] 同一账单重复点击、重启重试或网络超时不得造成重复写入。
- [ ] 保存失败时保留输入内容，提供重试和存为本地草稿。

### 5.5 验收标准

- 单手可在 10 秒内完成一笔常用支出。
- 收入、支出、转账和多币种金额在移动端与 Web 端结果一致。
- 连续点击保存不会生成重复流水。
- 断网记账、杀进程、重新打开、恢复网络后数据能够正确同步。
- 软键盘弹出、横竖屏切换和系统大字体下保存按钮不被遮挡。

## 6. 阶段 2：账单浏览与管理

### 6.1 月份与分组

- [x] 顶部支持左右切换月份、月份选择器和回到本月。
- [x] 账单按日期分组，显示每日收入、支出和结余小计。
- [x] 月份顶部显示总收入、总支出、结余和账单数量。
- [x] 空月份显示 Neo Ledger 风格空状态和“记一笔”入口。

### 6.2 搜索与筛选

- [ ] 搜索标题、商户、备注、分类和标签。
- [ ] 按账户、分类、类型、金额区间、日期和同步状态筛选。
- [ ] 支持多个筛选条件组合、清除筛选和最近筛选记录。
- [x] 搜索和筛选结果与服务端查询口径一致，不只过滤当前 50 条缓存。

### 6.3 详情与操作

- [ ] 原生账单详情页展示全部字段、附件、来源和同步信息（基础详情、来源和同步信息已完成，等待附件字段）。
- [x] 编辑时进行版本冲突检查，不能静默覆盖另一终端的修改。
- [ ] 左滑显示编辑/删除，长按进入多选模式。
- [ ] 删除后显示短时撤销条；服务端失败时自动恢复并解释原因。
- [ ] 支持复制账单、批量分类、批量标签和批量删除。
- [ ] 自动识别账单显示“来源、识别文本、置信度和修正记录”。

### 6.4 验收标准

- 任意月份可在两次操作内到达。
- 搜索服务端历史账单不会受本地分页限制。
- 编辑、删除和撤销在 Web/Windows/macOS 上同步一致。
- 两台设备同时编辑同一账单时能看到明确冲突，不出现重复或静默丢失。

## 7. 阶段 3：首页与日常决策

### 7.1 首页信息层级

- [ ] 顶部显示当前账本、月份、同步状态、通知和头像。
- [ ] 主卡显示本月收入、支出、结余，并支持隐藏金额。
- [ ] 显示本周/近 7 日支出趋势和同比/环比说明。
- [ ] 显示预算进度、即将超支分类和可操作提醒。
- [ ] 显示待确认自动账单、还款提醒、订阅和周期账单。
- [ ] 最近账单显示有限条目，并提供进入完整列表的入口。

### 7.2 模块管理

- [ ] 用户可开启、关闭和排序首页模块。
- [ ] 首页模块设置跨设备同步。
- [ ] 无数据模块使用简洁空状态，不展示无意义的 `0.00` 大卡片。
- [ ] 首页刷新只拉取发生变化的数据，避免重复请求全部高级接口。

### 7.3 验收标准

- 用户打开应用后能在首屏确认余额、当月支出、预算风险和待处理事项。
- 首页滚动、下拉刷新和模块重排保持 60 fps 目标体验。
- 隐藏金额后，应用切后台、任务预览和重新进入不会短暂泄露金额。

## 8. 阶段 4：账户、转账与资产

### 8.1 账户

- [ ] 原生账户列表按资产、负债、投资和现金流分组。
- [ ] 新增、编辑、停用和排序账户。
- [ ] 显示余额、币种、最近流水和待同步状态。
- [ ] 余额校准必须生成可追踪调整记录，不能直接无记录改余额。

### 8.2 转账

- [ ] 实现账户间转账，支持手续费、日期、备注和多币种换算。
- [ ] 转出和转入必须作为一个原子操作保持守恒。
- [ ] 转账详情可从任一关联账户进入，编辑/删除同步影响两端。

### 8.3 资产与负债

- [ ] 原生净资产首页显示总资产、总负债和净资产。
- [ ] 管理房产、车辆、贵金属、数码设备、收藏和其他资产。
- [ ] 支持手动估值、折旧参数、购入价、当前价值和处置。
- [ ] 显示资产趋势、负债率、账户结构和未来一年购买力。
- [ ] 信用账户支持账单日、还款日和免息期提醒。

### 8.4 验收标准

- 转账前后总净资产守恒，手续费除外。
- 账户余额、流水汇总、净资产和 Web 端一致。
- 删除或停用账户时不能产生孤立流水。
- 多币种账户使用同一汇率快照并明确标注更新时间。

## 9. 阶段 5：预算、周期与计划

### 9.1 预算

- [ ] 月总预算与分类预算原生管理。
- [ ] 显示预算剩余、使用比例、日均可用和超支预测。
- [ ] 账单“不计入预算”标记在所有终端保持一致。
- [ ] 支持预算结转规则和月初自动重置。

### 9.2 周期业务

- [ ] 周期记账任务：频率、执行日、账户、分类、提醒和暂停。
- [ ] 订阅管理：金额、续费日、涨价记录和取消提醒。
- [ ] 分期管理：总额、期数、手续费、已还和剩余期数。
- [ ] 信用卡还款和固定支出进入统一日历。

### 9.3 储蓄目标

- [ ] 存钱目标支持目标金额、期限、进度和关联账户。
- [ ] 入金、撤回和完成均产生可追踪记录。
- [ ] 首页只显示需要用户处理的计划，不堆叠全部项目。

### 9.4 验收标准

- 周期任务跨时区、跨月末和闰年运行结果一致。
- 重试不会重复生成周期账单。
- 预算、分期、订阅和储蓄目标计算与服务端测试向量一致。

## 10. 阶段 6：自动记账与智能导入

### 10.1 Android 自动记账

- [ ] 把现有通知监听与无障碍能力整合进正式移动端设置流程。
- [ ] 使用分步骤授权引导，明确说明每项权限用途和关闭方式。
- [ ] 识别事件先进入待确认页，展示金额、商户、来源和识别证据。
- [ ] 提供应用白名单、重复检测、资产/分类匹配和识别日志。
- [ ] 支持自启动、电池优化、通知栏状态和厂商 ROM 诊断。
- [ ] 不自动点击支付、不读取无关页面、不绕过用户确认付款。

### 10.2 iOS/iPadOS 合规入口

- [ ] Share Extension 接收用户主动分享的支付结果或账单图片。
- [ ] Shortcuts/App Intents 支持金额、商户、分类和时间预填。
- [ ] 支持剪贴板和文件导入，但必须由用户主动触发和确认。
- [ ] 清楚标注 iOS 不能后台读取其他应用通知和界面的系统限制。

### 10.3 截图 AI 记账

- [ ] 相册或系统分享进入原生截图确认流程。
- [ ] 先在本地 OCR，过滤订单号、手机号、银行卡号、身份证号和地址。
- [ ] 仅将脱敏文本提交给用户配置的 AI 服务。
- [ ] 展示原图、本地提取文本、脱敏结果和最终识别字段。
- [ ] 用户确认后才写入正式账本；原图保存策略由用户选择。

### 10.4 导入

- [ ] 支持微信、支付宝、银行 CSV/Excel 和通用 JSON 导入。
- [ ] 导入前预览、字段映射、重复检测和错误行修正。
- [ ] 大文件分页处理，支持取消和失败后继续。

### 10.5 验收标准

- 真机覆盖微信、支付宝和至少三种主流厂商 Android ROM。
- 识别、待确认、已入账、重复和失败数量可追踪且守恒。
- 截图脱敏自动化测试覆盖常见订单号、手机号和银行卡号。
- iOS 分享扩展和快捷指令通过真机与 TestFlight 验收。

## 11. 阶段 7：我的、设置与安全

### 11.1 账号资料

- [ ] 原生头像选择、裁剪、压缩、上传和删除。
- [ ] 修改昵称、绑定/更换邮箱和验证码流程。
- [ ] 账号注册日期、登录方式和当前账本信息。
- [ ] 退出登录前明确说明未同步数据状态。

### 11.2 应用设置

- [ ] 主题、跟随系统、深色模式和高对比度模式。
- [ ] 金额千分位、货币符号、默认货币和金额颜色规则。
- [ ] 秒开记账、连续记账、震动、首页模块和分类展开设置。
- [ ] 分类、标签、币种、导入、导出和清除数据入口。

### 11.3 安全与设备

- [ ] 应用锁、生物识别、后台模糊和截屏保护策略。
- [ ] 登录设备列表、撤销设备和安全审计记录。
- [ ] Passkey/MFA 的原生管理或安全的系统浏览器授权回跳。
- [ ] 数据导出、备份、恢复和注销账号需要二次确认与审计。

### 11.4 关于与支持

- [ ] 版本号、更新日志、检查更新、隐私政策和服务条款。
- [ ] 问题反馈自动附带脱敏后的版本、平台和错误信息。
- [ ] Android 使用可信 APK/商店更新；iOS 使用 TestFlight/App Store。

### 11.5 验收标准

- 更换头像在 Android、iOS、Web、Windows 和 macOS 同步显示。
- 生物识别失败有密码回退，不会把用户永久锁在应用外。
- 登出和会话撤销不删除未同步队列，注销账号流程除外。
- 所有破坏性操作明确显示影响范围并需要确认。

## 12. 阶段 8：精品化、平板与无障碍

### 12.1 动效与触觉

- [ ] 页面进入、卡片展开、金额变化和保存完成使用统一动画曲线。
- [ ] 关键成功、警告和失败使用不同强度触觉反馈，并允许关闭。
- [ ] 避免长动画阻塞记账；系统“减少动态效果”开启时自动降级。

### 12.2 响应式布局

- [ ] 小屏手机不溢出，底部操作保持单手可达。
- [ ] 平板竖屏使用双列，横屏使用 Navigation Rail 与主从详情。
- [ ] 支持分屏、窗口缩放、横竖屏切换和外接键盘。
- [ ] Android 系统返回、iOS 侧滑返回和弹层关闭行为符合平台习惯。

### 12.3 无障碍与国际化基础

- [ ] 所有图标按钮有语义标签，金额不只依赖颜色表达。
- [ ] 支持系统字体放大、读屏顺序、触控目标和高对比度。
- [ ] 日期、数字、货币和时区使用统一格式化层。
- [ ] 中文文案不写死在页面结构中，为后续本地化预留资源文件。

### 12.4 性能与稳定性

- [ ] 首屏、账单列表和记账页建立性能基线。
- [ ] 长账单列表使用分页和虚拟化，不一次加载全部附件。
- [ ] 图片压缩、缓存和清理策略限制本地磁盘占用。
- [ ] 崩溃和 API 错误日志默认脱敏，用户可选择是否提交。

### 12.5 验收标准

- 主流设备滚动和记账交互达到稳定流畅目标，无明显掉帧。
- 320 px 宽度、系统最大字体和键盘弹出时无关键控件遮挡。
- TalkBack/VoiceOver 可完成登录、记账、编辑、删除和登出核心路径。
- 1 万条流水下搜索、分页和滚动仍可用。

## 13. 阶段 9：真机矩阵与正式发布

### 13.1 真机矩阵

- [ ] Android：至少覆盖 Pixel/原生系统和两个主流国产厂商系统。
- [ ] iPhone：覆盖当前主版本和一个仍支持的旧系统版本。
- [ ] 平板：至少一台 Android 平板和一台 iPad，覆盖横竖屏与分屏。
- [ ] 弱网、断网、切后台、杀进程、重启、低电量和存储不足场景。

### 13.2 跨端同步矩阵

- [ ] 手机离线记账，联网后 Web/Windows/macOS 出现且不重复。
- [ ] 桌面编辑账单，移动端在前台和后台恢复后正确更新。
- [ ] 多设备同时编辑出现可解释冲突。
- [ ] 删除、撤销、转账、预算、附件和头像跨端一致。

### 13.3 发布与回滚

- [ ] Android APK/AAB 使用连续正式签名，验证覆盖升级和数据保留。
- [ ] iOS 配置 Apple Developer、签名、Provisioning 和 TestFlight。
- [ ] 更新检查只选择适合当前平台和渠道的安装资产。
- [ ] 发布说明列出数据迁移、权限变化、已知问题和回滚方式。
- [ ] 正式发布前完成备份恢复和上一稳定版本回退演练。

### 13.4 `2.0.0` 发布条件

以下条件同时满足后，才允许将移动端标记为精品正式版本：

1. 阶段 0–8 的所有 P0/P1 项目完成并有测试证据。
2. Android 和 iOS 核心路径完成真机验收。
3. 账号、金额、账户余额、净资产和同步结果与 Web 端一致。
4. 不存在未关闭的崩溃、数据丢失、重复记账或越权访问问题。
5. 升级、签名、安装、回滚和隐私权限说明完整。

## 14. 每项功能的完成定义

每个勾选项只有同时满足以下条件才能标为完成：

- 有可操作的原生界面，不是静态卡片或“即将上线”提示。
- 使用正式 API 和真实数据，不是演示数据或仅本地状态。
- 支持加载、空数据、错误、离线和权限不足状态。
- 对写入操作具备防重复、失败恢复和必要的冲突处理。
- 有对应单元测试、Widget 测试或集成测试。
- Android 构建通过；涉及 iOS 的功能同时通过 iOS 构建。
- 至少完成一次目标平台真机操作记录。
- 文档、功能矩阵和版本说明同步更新。

## 15. 每个阶段的固定验证命令

在 `apps/native` 目录执行：

```bash
/Users/peng/Development/flutter/bin/dart format lib test
/Users/peng/Development/flutter/bin/flutter analyze
/Users/peng/Development/flutter/bin/flutter test
/Users/peng/Development/flutter/bin/flutter build apk --debug
/Users/peng/Development/flutter/bin/flutter build ios --simulator
```

同时执行仓库现有的 Web/API 测试，确认共享服务端没有回归。正式发布阶段还必须执行签名构建、校验和、安装升级和回滚测试，不能用 debug 构建代替。

## 16. 执行记录模板

每完成一个阶段，在本节新增一条记录：

```text
阶段：
版本：
提交：
完成项：
未完成项：
Android 构建：
iOS 构建：
真机型号与系统：
跨端同步结果：
已知问题：
回滚方式：
验收人：
验收日期：
```

本计划是移动端后续开发的执行顺序和验收依据。若产品范围发生变化，应先修改本计划和功能矩阵，再进入代码实现，避免不同平台出现功能口径漂移。
